# Host -> container port mapping (change if needed)
BACKEND_HOST_PORT=7000
# The backend image actually listens on 5000 inside the container
BACKEND_CONTAINER_PORT=5000
FRONTEND_HOST_PORT=8080
# The frontend image actually serves on 9000 inside the container
FRONTEND_CONTAINER_PORT=9000

# API URL consumed by the frontend (change to match host/port or container network)
API_URL=http://localhost:7000
